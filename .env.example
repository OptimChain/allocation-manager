# Twelve Data API Key (required for portfolio comparison)
# Get your free API key at: https://twelvedata.com/
VITE_TWELVE_DATA_API_KEY=your_api_key_here

# =============================================================================
# Robinhood Credentials (required for Trade tab)
# =============================================================================
# Your Robinhood account email/username
RH_USER=your_robinhood_email_here

# Your Robinhood account password
RH_PASS=your_robinhood_password_here

# Optional: Custom device token (UUID format)
# Use this to bypass rate limits by appearing as a new device
# Generate one with: uuidgen | tr '[:upper:]' '[:lower:]'
# RH_DEVICE_TOKEN=xxxxxxxx-xxxx-xxxx-xxxx-xxxxxxxxxxxx

# =============================================================================
# Plaid Credentials (required for brokerage connection via Plaid Link)
# =============================================================================
# Get credentials at: https://dashboard.plaid.com/
PLAID_CLIENT_ID=your_plaid_client_id_here
PLAID_SECRET=your_plaid_secret_here
# Options: sandbox, production
PLAID_ENV=sandbox

# =============================================================================
# Local Testing Setup
# =============================================================================
# 1. Copy this file to .env:
#    cp .env.example .env
#
# 2. Fill in your credentials in .env (never commit this file!)
#
# 3. Start the dev server:
#    npm run dev:clean
#
# 4. Test authentication flow:
#    # Initiate login (triggers device verification)
#    curl "http://localhost:3000/.netlify/functions/robinhood-portfolio?action=auth"
#
#    # Check auth status
#    curl "http://localhost:3000/.netlify/functions/robinhood-portfolio?action=status"
#
#    # After approving in email/SMS, verify:
#    curl "http://localhost:3000/.netlify/functions/robinhood-portfolio?action=verify"
#
#    # If MFA code required:
#    curl "http://localhost:3000/.netlify/functions/robinhood-portfolio?action=mfa&code=123456"
#
#    # Once authenticated, access portfolio:
#    curl "http://localhost:3000/.netlify/functions/robinhood-portfolio?action=portfolio"
#
# =============================================================================
# Security Notes
# =============================================================================
# - NEVER commit .env files to git
# - Robinhood may require device verification on first login
# - Tokens are cached in memory and expire after ~24 hours
# - Cold starts (after ~15-20min idle) will require re-authentication
